package speeder.jpa.persistence;

/**
 * Clase base para los objetos filter de las entidades.
 * Un objeto filter son clases utiles para implementar consultas
 * personalizadas por el usuario para devolver datos del modelo.
 */
public abstract class AbstractFilter implements EntityFilter
{
}
